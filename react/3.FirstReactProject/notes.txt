- We don't have to manually setup a react project everytime we want to start a new project
- We have some third party libraries that we can use to make our lives easier
- For example, one of the tool is called `create-react-app` , onther one is called `vite`